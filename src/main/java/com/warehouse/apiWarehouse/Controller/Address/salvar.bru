meta {
  name: salvar
  type: http
  seq: 3
}

post {
  url: http://localhost:8080/api/address
  body: json
  auth: inherit
}

body:json {
  {
    "street":"q",
    "city":"adda"
  }
}

settings {
  encodeUrl: true
}
